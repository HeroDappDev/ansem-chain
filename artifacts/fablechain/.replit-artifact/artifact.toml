kind = "web"
previewPath = "/"
title = "Ansem Chain"
version = "1.0.0"
id = "artifacts/fablechain"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24417

[services.development]
run = "pnpm --filter @workspace/ansemchain run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ansemchain", "run", "build" ]
publicDir = "artifacts/fablechain/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24417"
BASE_PATH = "/"
